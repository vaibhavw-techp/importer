spring.application.name=importer
mybatis.mapper-locations=classpath:mapper/*.xml

spring.datasource.driver-class-name=org.postgresql.Driver
server.port=8083

aws.region=eu-north-1
aws.secretsmanager.jwt.secret=intern/dev/importer

#spring.profiles.active=dev  #Run Configuration -> Modify -> Add VM Options -> -Dspring-profiles.active=dev

